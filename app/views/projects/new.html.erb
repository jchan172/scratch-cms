<% provide(:mode, 'management') %>
<% provide(:title, 'Create Project') %>

<div class="container-fluid">
  <div class="row-fluid">
    <%= render 'layouts/sidebar' %>
    <script type="text/javascript">
      $('#projects-link').removeClass("inactive").addClass("active") // change the "inactive" class to "active"
    </script>

    <div class="span8">
      <h1>Create a New Project</h1>

        <%= form_for(@project) do |f| %>
          <%= render 'shared/error_messages', object: f.object %>
          
            <%= f.label :title %>
            <%= f.text_field :title %>

          <div class="redactor_content">
            <%= f.text_area :content, placeholder: "Project content goes here...", :class => "redactor"%>
          </div>

          <br>

          <%= f.submit "Create Project", class: "btn btn-large btn-success" %>
        <% end %>

    </div>
  </div>
</div>
<%# add some space so that footer bar doesn't cover anything %>
<br>
<br>