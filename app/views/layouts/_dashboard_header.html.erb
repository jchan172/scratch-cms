<div class="navbar navbar-fixed-top">
	<div class="navbar-inner">
		<div class="container-fluid">
			<button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="brand" href='/'>Scratch CMS Home</a>
			<div class="nav-collapse collapse">
				<p class="navbar-text pull-right">
					
				</p>
				<ul class="nav pull-right">
					<% if signed_in? %>
						<li class="dropdown">
							<%= link_to '#', :class => "dropdown-toggle", "data-toggle" => "dropdown" do %>
	           	  				Logged in as <%= current_user.name %> <b class="caret"></b>
           	  				<% end %>
           	  				<ul class="dropdown-menu">
           	  					<li>
           	  						<%# deprecated way of rendering users/edit, keeping code for reference %>
           	  						<%#= link_to 'Account Settings', dashboard_path(:link => 'settings'), remote: true %>
           	  						<%= link_to 'Account Settings', edit_user_path(:id => current_user.id) %>
           	  					</li>
           	  					<li>
           	  						<%= link_to "Sign Out", logout_path, method: :delete %>
           	  					</li>
           	  				</ul>
						</li>
					<% end %>
				</ul>
			</div>
			<!--/.nav-collapse -->
		</div>
	</div>
</div>
